const movies = require('../models/movies');

const getMoviesByCategory = async (categories, published) => {

};

const createMovie = async (categories, published) => {
    
};

module.exports = {getMoviesByCategory}